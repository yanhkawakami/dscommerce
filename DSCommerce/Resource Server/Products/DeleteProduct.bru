meta {
  name: DeleteProduct
  type: http
  seq: 1
}

delete {
  url: http://localhost:8080/products/26
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
