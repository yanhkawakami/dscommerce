meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{ashost}}/oauth2/token
  body: formUrlEncoded
  auth: basic
}

auth:basic {
  username: {{client-id}}
  password: {{client-secret}}
}

body:form-urlencoded {
  username: {{username}}
  password: {{password}}
  grant_type: password
  ~: 
}

body:multipart-form {
  : 
}

script:post-response {
  let data = res.body;
  bru.setEnvVar("token", data.access_token);
}

settings {
  encodeUrl: true
  timeout: 0
}
